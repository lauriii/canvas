diff -ruN --exclude=node_modules --exclude=.env --exclude=.env.* --exclude=dist --exclude=.astro --exclude=.next --exclude=.output --exclude=out --exclude=*.log --exclude=package-lock.json --exclude=.agents --exclude=.claude --exclude=skills-lock.json --exclude=.nuxt --exclude=.canvas headless-templates/nuxt/nuxt.config.ts spike-nuxt/nuxt.config.ts
--- headless-templates/nuxt/nuxt.config.ts	2026-08-27 17:23:53
+++ spike-nuxt/nuxt.config.ts	2026-08-27 17:43:17
@@ -15,6 +15,26 @@
         exclude: /node_modules\/(?!@drupal-canvas\/headless)/,
       },
     },
+    // SSG spike: explicit published paths (enumerated from JSON:API) and an
+    // ignore for the adapter's editor-only preview page, which 404s without
+    // a componentId query and otherwise aborts `nuxi generate`.
+    prerender: {
+      routes: [
+        '/about',
+        '/careers',
+        '/register',
+        '/login',
+        '/home',
+        '/services',
+        '/blog',
+        '/node/2',
+        '/node/3',
+        '/node/5',
+        '/node/8',
+        '/node/11',
+      ],
+      ignore: ['/api/canvas/component-preview'],
+    },
   },
   app: {
     head: {
