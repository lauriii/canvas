---
'@drupal-canvas/vite-plugin': patch
'@drupal-canvas/workbench': patch
---

Fetch public Canvas site metadata without credentials, including when tooling has an OAuth token configured. Authenticated data requests and write operations are unchanged.
