---
"@drupal-canvas/headless-angular": minor
---

Make the shared Headless SDK a regular dependency, matching the other adapters.
Release Angular's shared editor-origin policy support as a minor update rather
than allowing the SDK's minor release to trigger a peer-dependency major bump.
