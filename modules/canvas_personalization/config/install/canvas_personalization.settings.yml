# Geolocation segment conditions read the visitor's location from request
# headers set by a CDN or reverse proxy at the edge; Drupal never resolves IP
# addresses itself. Point these at whatever headers your edge provides.
country_header: 'X-Country-Code'
region_header: 'X-Region-Code'
