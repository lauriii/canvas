name: 'Experience Builder Test Site'
description: 'Site for running tests against.'
type: 'XB'

config:
  strict: true
  actions:
    system.site:
      simpleConfigUpdate:
        page.front: /homepage
        name: 'Drupal'
