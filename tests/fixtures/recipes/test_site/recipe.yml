name: 'Experience Builder Test Site'
description: 'Site for running tests against.'

config:
  strict: true
  actions:
    system.site:
      simpleConfigUpdate:
        page.front: /homepage
        name: 'Drupal'
